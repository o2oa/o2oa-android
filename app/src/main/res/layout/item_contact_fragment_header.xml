<?xml version="1.0" encoding="utf-8"?>
<LinearLayout  xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="40dp"
    android:layout_marginTop="@dimen/spacing_small"
    android:layout_marginStart="@dimen/spacing_small"
    android:layout_marginEnd="@dimen/spacing_small"
    android:orientation="vertical">
    <View
        android:id="@+id/view_item_contact_fragment_header_top"
        android:layout_width="match_parent"
        android:layout_height="10dp"
        android:background="@drawable/background_white_top_radius"/>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/white">
       <ImageView
           android:id="@+id/image_item_contact_fragment_header_icon"
           android:layout_width="20dp"
           android:layout_height="20dp"
           android:layout_centerVertical="true"
           android:layout_marginStart="@dimen/spacing_small"
           android:src="@mipmap/icon_contact_my_department"/>
        <TextView
            android:id="@+id/tv_item_contact_fragment_header_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toEndOf="@id/image_item_contact_fragment_header_icon"
            android:layout_marginStart="@dimen/spacing_small"
            tools:text="我的部门"
            android:textSize="15sp"
            android:textColor="@color/z_color_text_primary_dark"/>
    </RelativeLayout>
    <View
        android:id="@+id/view_item_contact_fragment_header_bottom"
        android:layout_width="match_parent"
        android:layout_height="10dp"
        android:visibility="gone"
        android:background="@drawable/background_white_bottom_radius"/>
</LinearLayout>
